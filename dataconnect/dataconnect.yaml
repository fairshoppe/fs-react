specVersion: "v1beta"
serviceId: "fs-react"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fs-react-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
